websocket {
     port: 8080
     tls: {
        cert_file: "/etc/letsencrypt/live/${VITE_DOMAIN_NAME}/fullchain.pem"
        key_file: "/etc/letsencrypt/live/${VITE_DOMAIN_NAME}/privkey.pem"
        timeout: "5s"
     }
}
